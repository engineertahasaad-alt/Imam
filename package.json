{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "apk": "pnpm --filter @workspace/imam apk",
    "apk:dev": "pnpm --filter @workspace/imam apk:dev",
    "aab": "pnpm --filter @workspace/imam aab",
    "submit": "pnpm --filter @workspace/imam submit",
    "update": "pnpm --filter @workspace/imam update",
    "update:preview": "pnpm --filter @workspace/imam update:preview",
    "update:dev": "pnpm --filter @workspace/imam update:dev"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
